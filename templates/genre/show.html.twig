{% extends 'base.html.twig' %}

{% block body %}
<h1 class="mb-1">Genre: {{ genre.name }}</h1>
<hr class="mb-1">
<ul class="genre__actions">
    <li><a href="{{ path('genre_delete', { id: genre.id }) }}">Delete</a></li>
    <li><a href="{{ path('genre_update', { id: genre.id }) }}">Update</a></li>
</ul>
{% endblock %}
